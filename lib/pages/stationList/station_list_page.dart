import 'package:flutter/material.dart';

class StataionList extends StatelessWidget {
  const StataionList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Station List')),
      body: Center(child: const Text('Welcome to the Station List Page!')),
    );
  }
}
